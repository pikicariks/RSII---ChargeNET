# RSII---ChargeNET

## Test Login Accounts

- Admin: `admin@chargenet.com`
- Driver: `demo@chargenet.com`

## Password Note

Seeded users above exist in the database seed, but their password hashes are placeholders in code.
Use the credentials provided in your local `.env-tajne.zip` setup, or register fresh test users if seeded passwords are not available in your environment.